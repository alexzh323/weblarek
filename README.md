https://github.com/alexzh323/weblarek

# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные
В ходе анализа проекта было установлено, что в приложении используются две основные сущности, описывающие данные: товар и покупатель.

#### Интерфейс товара (IProduct)
Интерфейс содержит все данные о товаре. Необходим для описания данных о товаре, полученных с сервера. 

```typescript
interface IProduct {
  id: string;          // уникальный номер товара
  description: string; // полное описание товара
  image: string;       // путь к изображению товара
  title: string;       // название товара
  category: string;    // категория товара
  price: number | null;// цена товара, для бесценных товаров равна null
}
```

#### Интерфейс покупателя (IBuyer)
Интерфейс содержит все данные о покупателе. Задает структуру для данных покупателя, собираемых из форм оформления заказа.

```typescript
type TPayment = 'card' | 'cash';

interface IBuyer {
  payment: TPayment | null;   // тип платежа: наличные ('cash'), карта ('card') или null, если поле не заполнено
  email: string;       // электронная почта покупателя
  phone: string;       // телефонный номер покупателя
  address: string;     // адрес доставки покупателя
}
```

#### Классы для хранения данных
Для учёта данных в приложении используются три класса, которые будут разделены между собой по смыслу и зонам ответственности:
ProductData - хранение товаров, которые можно купить в приложении;
CartData - хранение товаров, которые пользователь выбрал для покупки;
OrderData - данные покупателя, которые тот должен указать при оформлении заказа.

##### Класс ProductData
Класс служит для хранения всех товаров, которые можно купить в приложении, а также текущего товара, выбранного на данный момент.

Конструктор:  
`constructor()` создает пустой массив для списка товаров.

Поля класса:
`protected products: IProduct[]` - массив всех доступных товаров. Используется для отображения каталога на главной странице.
`protected currentProduct: IProduct | null` - текущий товар, выбранный для детального просмотра в модальном окне.
Поля класса защищенные, с ними можно взаимодействовать только через методы.

Методы класса:
`setProducts(products: IProduct[]): void` - сохраняет массив товаров в поле `products`. Полностью заменяет предыдущие элементы массива новыми данными.
`getProducts(): IProduct[]` — возвращает массив всех товаров из модели.
`getProduct(id: string): IProduct | null` - возвращает товар по его уникальному `id`. Если товар с таким идентификатором не найден, возвращает `null`.
`setCurrentProduct(product: IProduct): void` - сохраняет выбранный товар в поле `currentProduct` для последующего отображения в модальном окне.
`getCurrentProduct(): IProduct | null` - возвращает товар, выбранный в данный момент для детального просмотра, или `null`, если ни один товар не выбран.

##### Класс CartData 
Класс служит для хранения всех товаров, добавленных в корзину. Необходим для всех манипуляций с корзиной: добавления/удаления товаров, очистки корзины, проверки наличия в корзине конкретного товара, а также получения общего количества товаров в корзине и их суммарной стоимости. 

Конструктор: 
`constructor()` - создает пустой массив для списка товаров в корзине. 

Поля класса:
`protected cartProducts: IProduct[]` - массив всех товаров, добавленных в корзину. Используется для отображения списка товаров в корзине. Поле защищенное, с ним можно взаимодействовать только через методы. 

Методы класса: 
`getCartProducts(): IProduct[]` - возвращает массив всех товаров в корзине. `addProduct(product: IProduct): void` - добавляет товар, полученный в параметре, в массив товаров корзины. 
`removeProduct(product: IProduct): void` - удаляет товар, полученный в параметре, из массива товаров корзины. 
`clearCart(): void` - удаляет все товары из корзины, превращая `cartProducts` в пустой массив. 
`getTotalPrice(): number` - вычисляет суммарную цену всех товаров в корзине и возвращает её. Если корзина пуста, возвращает `0`. 
`getTotalNumber(): number` - возвращает количество товаров в корзине, то есть длину массива `cartProducts`. 
`checkCartProduct(id: string): boolean` - проверяет наличие товара в корзине по его `id`, полученному в параметр метода, и возвращает `true` или `false`.

##### Класс OrderData 
Класс служит для хранения всех данных покупателя при оформлении заказа. Необходим для сохранения, получения, удаления и валидации данных покупателя. 

Конструктор: 
`constructor()` - инициализирует поля класса начальными пустыми значениями (`''` и `null` для типа платежа), данные заполняются с помощью методов по мере заполнения формы. 

Поля класса: 
`protected payment: TPayment | null` - тип платежа: наличные ('cash'), карта ('card') или `null`, если поле не заполнено. 
`protected email: string` - электронная почта покупателя. 
`protected phone: string` - телефонный номер покупателя. 
`protected address: string` - адрес доставки покупателя. 
Поля класса защищенные, с ними можно взаимодействовать только через методы.

Методы класса:
`setField<K extends keyof IBuyer>(field: K, value: IBuyer[K]): void` - универсальный метод сохранения данных для любого поля покупателя. Принимает название поля и его новое значение. Благодаря дженерикам, тип значения строго синхронизируется с типом выбранного поля интерфейса `IBuyer`.
`getFields(): IBuyer` - возвращает объект со всеми текущими данными покупателя.
`clearFields(): void` - очищает все данные покупателя, возвращая поля к их начальным пустым значениям (`''`).
`getInvalidFields(): TOrderValidationResult` - проводит валидацию заполненных полей. Поле считается невалидным, если его значение является пустой строкой. Возвращает объект, где ключами являются названия невалидных полей, а значениями - текст ошибок. Если все поля заполнены верно, возвращает пустой объект. 
Вспомогательные типы данных: 
```typescript 
type TOrderValidationResult = {
payment?: string; // Текст ошибки для способа оплаты 
address?: string; // Текст ошибки для адреса доставки
email?: string; // Текст ошибки для электронной почты
phone?: string; // Текст ошибки для номера телефона
}
```

### Слой коммуникации
#### Класс LarekApi 
Класс использует методы класса Api для отправки специализированных запросов на сервер.

Конструктор:
`constructor(api: IApi)` - принимает экземпляр класса Api, соответствующий интерфейсу IApi, и сохраняет его в своем поле.

Поля класса:
`protected api: IApi` - защищенное поле, необходимое для дальнейших сетевых запросов.

Методы класса:
`getProductList(): Promise<IProductListResponse>` - выполняет GET-запрос на эндпоинт /product/. Возвращает промис с объектом, содержащим массив товаров (`IProduct[]`) и их общее количество.
`orderProducts(order: IOrderRequest): Promise<IOrderResponse>` - выполняет POST-запрос на эндпоинт /order/. Передает в теле запроса данные о заказе и покупателе (на основе`IBuyer`), возвращает промис с ответом от сервера, подтверждающим успешную покупку.
Вспомогательные типы данных: 
```typescript 
type IProductListResponse = {
  total: number;
  items: IProduct[]; 
}
type IOrderRequest = IBuyer & {
  total: number;
  items: string[]; 
}
type IOrderResponse ={
  id: string;
  total: number;
}
```
### Компоненты слоя представления (View) 
Содержит классы, необходимые для корректного отображения HTML-элементов страницы и фиксации событий, вызванных действиями пользователя, для их дальнейшей обработки Презентером. 

#### Класс Header 
Класс является производным от класса `Component` и отвечает за отображение шапки сайта. Он управляет кнопкой корзины и счётчиком количества товаров. 

Конструктор: 
 `constructor(container: HTMLElement, protected events: IEvents)` - принимает корневой DOM-элемент хедера (в котором ищет дочерние элементы для заполнения полей класса) и экземпляр брокера событий для отправки сигналов в Презентер. В конструкторе устанавливается слушатель клика на кнопку корзины. 

Поля класса: 
`protected counterElement: HTMLElement` - элемент счётчика товаров. `protected basketButton: HTMLButtonElement` - кнопка открытия корзины. 

Методы класса: 
`set counter(value: number)` - сеттер, принимает число товаров и устанавливает его в качестве текстового содержимого для элемента `counterElement`. 

Вспомогательные интерфейсы для дженерика: 
```typescript 
interface IHeader { 
  counter: number; 
  } 
```

#### Класс Gallery
Класс является производным от класса `Component` и отвечает за отображение сетки товаров на главной странице.

Конструктор:
`constructor(container: HTMLElement)` - принимает корневой DOM-элемент (контейнер `<main class="gallery">`), в котором находит область для отрисовки карточек.

Поля класса:
`protected catalogField: HTMLElement` - DOM-элемент (область), в которую динамически выводятся карточки товаров каталога.

Методы класса:
`set catalog(value: HTMLElement[])` - сеттер, принимает массив готовых HTML-элементов карточек и вставляет их в контейнер `catalogField`.

Вспомогательные интерфейсы для дженерика: 
```typescript 
interface IGallery { 
  catalog: HTMLElement[]; 
}
```

#### Класс Basket
Класс является производным от класса `Component` и отвечает за отображение содержимого корзины (списка выбранных товаров и их итоговой стоимости), а также за управление кнопкой перехода к оформлению заказа.

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - принимает корневой DOM-элемент темплейта корзины (в котором ищет дочерние элементы для заполнения полей класса) и экземпляр брокера событий для отправки сигналов в Презентер. В конструкторе устанавливается слушатель клика на кнопку оформления заказа, генерирующий событие начала оформления.

Поля класса:
`protected basketCatalogField: HTMLElement` - область для отображения списка товаров в корзине.
`protected basketTotal: HTMLElement` - элемент для отображения общей стоимости товаров в корзине.
`protected orderButton: HTMLButtonElement` - кнопка перехода к оформлению заказа.

Методы класса:
`set catalog(value: HTMLElement[])` - сеттер, принимает массив HTML-элементов (строк товаров в корзине) и отрисовывает их внутри `basketCatalogField`.
`set total(value: number)` - сеттер, принимает число и устанавливает его в качестве текстового содержимого для элемента `basketTotal`.

Вспомогательные интерфейсы для дженерика: 
```typescript 
interface IBasket { 
  catalog: HTMLElement[]; 
  total: number;
}
```
#### Класс Modal
Класс является производным от класса `Component` и служит универсальной оболочкой для отображения, открытия и закрытия всех модальных окон на странице.

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - принимает корневой DOM-элемент модального окна (который одновременно выполняет роль подложки/оверлея) и экземпляр брокера событий. В конструкторе устанавливается слушатель клика на кнопку закрытия, а также слушатель на сам контейнер для закрытия окна при клике мимо контента (с остановкой всплытия событий на внутреннем контентном блоке). 

Поля класса:
`protected modalField: HTMLElement` - область внутри модального окна для динамического размещения контента.
`protected closeButton: HTMLButtonElement` - кнопка закрытия модального окна.

Методы класса:
`set content(value: HTMLElement)` - сеттер, принимает HTML-элемент и полностью заменяет им текущее содержимое внутри области `modalField`.
`open(): void` - открывает модальное окно путём добавления HTML-класса `modal_active`. Генерирует событие открытия модального окна в Презентер.
`close(): void` - закрывает модальное окно путём удаления HTML-класса `modal_active`. Генерирует событие закрытия модального окна в Презентер для последующего сброса состояний моделей.

Вспомогательные интерфейсы для дженерика: 
```typescript 
interface IModal { 
  content: HTMLElement; 
}
```

#### Класс Card
Абстрактный класс является производным от класса `Component` и служит основой для всех классов отображения, использующих карточку товара. Инкапсулирует общую логику отображения названия и цены.

Конструктор: 
`constructor(container: HTMLElement)` - принимает корневой DOM-элемент карточки товара (в котором ищет дочерние элементы для заполнения полей класса).

Поля класса:
`protected titleElement: HTMLElement `- область для отображения названия товара.
`protected priceElement: HTMLElement` - область для отображения цены товара.

Методы класса:
`set title(value: string)` - сеттер, принимает строку с именем товара и заменяет ей текстовое содержимое поля ` titleElement `.
`set price(value: number | null)` - сеттер, принимает значение для цены товара. Если передано число, отображает его в формате `X синапсов`. Если передано значение `null`, выводит текст `Бесценно`.

Вспомогательные интерфейсы для дженерика: 
```typescript 
interface ICard 
{ title: string; 
  price: number | null
}
 ```

#### Класс CardCatalog
Класс является производным от класса `Card` и служит для отображения карточки в списке на главной странице. Также обрабатывает событие клика на карточку для последующей передачи презентеру.

Конструктор: 
`constructor(container: HTMLElement, actions?: ICardActions)`- принимает корневой элемент темплейта карточки каталога и необязательный объект с обработчиками событий. Вешает слушатель клика на всю карточку товара, являющуюся кнопкой.

Поля класса:
`protected categoryElement: HTMLElement ` элемент для отображения категории товара.
`protected imageElement: HTMLImageElement `- элемент для отображения изображения товара.

Методы класса:
`set category (value: string)` - сеттер, принимает строку с категорией товара и заменяет ей текстовое содержимое поля `categoryElement`. Дополнительно может управлять CSS-классами для изменения цвета плашки в зависимости от категории товара.
`set image (value: string)`- сеттер, устанавливает ссылку на изображение и альтернативное описание с помощью родительского метода `setImage`.

Вспомогательные интерфейсы для дженерика и событий: 
```typescript 
interface ICardCatalog extends ICard { 
  category: string; 
  image: string; 
} 

interface ICardActions { 
  onClick: (event: MouseEvent) => void; 
} 
```

#### Класс CardPreview
Класс является производным от класса `Card` и служит для отображения детального описания карточки. Также обрабатывает событие клика на кнопку добавления товара в корзину для последующей передачи презентеру.

Конструктор: 
`constructor(container: HTMLElement, actions?: ICardActions)`- принимает корневой элемент темплейта превью карточки и необязательный объект с обработчиками событий. Вешает слушатель клика на кнопку добавления товара в корзину для вызова переданного метода `onClick`.

Поля класса:
`protected categoryElement: HTMLElement ` элемент для отображения категории товара.
`protected imageElement: HTMLImageElement `- элемент для отображения изображения товара.
`protected textElement: HTMLElement `- элемент для отображения полного описания товара.
`protected cardButtonElement: HTMLButtonElement ` - кнопка добавления товара в корзину.


Методы класса:
`set category (value: string)` - сеттер, принимает строку с категорией товара и заменяет ей текстовое содержимое поля `categoryElement`. Дополнительно может управлять CSS-классами для изменения цвета плашки в зависимости от категории товара.
`set image (value: string)`- сеттер, устанавливает ссылку на изображение и альтернативное описание с помощью родительского метода `setImage`.
`set text (value: string)`- сеттер, сеттер, принимает строку с описанием товара и заменяет ей текстовое содержимое поля ` textElement `.
`set disabled(value: boolean)` - изменяет активность кнопки `cardButtonElement` (блокирует её для бесценных товаров, у которых цена равна `null`).


Вспомогательные интерфейсы для дженерика и событий: 
```typescript interface ICardPreview extends ICard { 
  category: string; 
  image: string; 
  text: string
  disabled?: boolean
} 

interface ICardActions { 
  onClick: (event: MouseEvent) => void; 
} 
```

#### Класс CardBasket
Класс является производным от класса `Card` и служит для отображения карточки в корзине. Также обрабатывает событие клика на кнопку удаления товара из корзины для последующей передачи презентеру.

Конструктор: 
`constructor(container: HTMLElement, actions?: ICardActions)`- принимает корневой элемент темплейта карточки в корзине и необязательный объект с обработчиками событий. Вешает слушатель клика на кнопку удаления товара из корзины для вызова переданного метода `onClick`.

Поля класса:
`protected basketItemIndex: HTMLElement ` элемент для отображения порядкового номера товара в корзине.
`protected basketDeleteItemButtonElement: HTMLButtonElement ` - кнопка удаления товара из корзины.


Методы класса:
`set index (value: number)` - сеттер, принимает число с порядковым номером товара в корзине и заменяет им текстовое содержимое поля ` basketItemIndex`.


Вспомогательные интерфейсы для дженерика и событий: 
```typescript interface ICardBasket extends ICard { 
  index: number; 
} 

interface ICardActions { 
  onClick: (event: MouseEvent) => void; 
} 
```
#### Класс Form
Абстрактный класс, являющийся производным от класса `Component`. Служит базовым компонентом для всех форм в приложении (ввода адреса и контактных данных), инкапсулируя общую логику обработки ввода, валидации и отправки данных.

Конструктор:
`constructor(container: HTMLFormElement, protected events: IEvents)` - принимает корневой элемент формы и экземпляр брокера событий. Вешает универсальный слушатель на событие `input` (для отслеживания изменений в полях ввода) и слушатель на событие `submit` (для блокировки стандартного поведения браузера и отправки сигнала Презентеру).

Поля класса:
`protected submitButton: HTMLButtonElement` - кнопка отправки формы.
`protected errorsElement: HTMLElement` - элемент для отображения текста ошибок валидации под формой.

Методы класса:
`set valid(value: boolean)` - управляет активностью кнопки отправки `submitButton`. При `true` разблокирует кнопку, при `false` делает её неактивной.
`set errors(value: string)` - принимает строку с текстом ошибки и устанавливает её в качестве содержимого для `errorsElement`.

Вспомогательные интерфейсы для дженерика:
```typescript
interface IFormState {
  valid: boolean;
  errors: string;
}
```

#### Класс OrderForm
Класс является производным от класса `Form` и отвечает за отображение первого шага оформления заказа: выбора способа оплаты и ввода адреса доставки.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` - принимает элемент формы из темплейта и экземпляр брокера событий. Находит уникальные кнопки выбора оплаты и инпут адреса. Устанавливает текст кнопки отправки (`submitButton`) в значение `Далее`. Навешивает слушатели клика на кнопки выбора оплаты для отправки сигналов в Презентер.

Поля класса:
`protected cardButton: HTMLButtonElement` - кнопка выбора оплаты картой.
`protected cashButton: HTMLButtonElement` - кнопка выбора оплаты наличными при получении.

Методы класса:
`set address(value: string)` — устанавливает переданную строку в качестве значения для инпута адреса доставки. Необходим для быстрой очистки данных и потенциальной расширяемости функционала приложения.
`set payment(value: TPayment | null)` - принимает текущий статус выбора оплаты. Переключает CSS-класс активности `button_alt-active` на соответствующей кнопке. Если передано значение `null`, снимает активность с обеих кнопок.

Вспомогательные интерфейсы для дженерика:
```typescript
interface IOrderForm extends IFormState {
  address: string;
  payment: TPayment | null;
}
```

#### Класс ContactsForm
Класс является производным от класса `Form` и отвечает за отображение второго шага оформления заказа: ввода контактных данных покупателя (Email и телефона).

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` - принимает элемент формы из темплейта и экземпляр брокера событий. Находит инпуты электронной почты и номера телефона. Устанавливает текст кнопки отправки (`submitButton`) в значение `Оплатить`.

Методы класса (сеттеры):
`set email(value: string)` - устанавливает переданную строку в качестве значения для инпута электронной почты.
`set phone(value: string)` - устанавливает переданную строку в качестве значения для инпута номера телефона.

Вспомогательные интерфейсы для дженерика:
```typescript
interface IContactsForm extends IFormState {
  email: string;
  phone: string;
}
```

#### Класс Success
Класс является производным от класса `Component` и отвечает за отображение финального окна с подтверждением успешного оформления заказа.

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - принимает корневой DOM-элемент темплейта успешного заказа и экземпляр брокера событий. В конструкторе устанавливается слушатель клика на кнопку «За новыми покупками!», генерирующий событие успешного завершения заказа для Презентера.

Поля класса:
`protected successTotalField: HTMLElement` - элемент для отображения итоговой суммы, списанной за заказ.
`protected successCloseButton: HTMLButtonElement` - кнопка закрытия окна и возврата к покупкам.

Методы класса (сеттеры):
`set total(value: number)` - принимает число (итоговую стоимость) и устанавливает его в элемент `successTotalField` в формате `Списано X синапсов`.

Вспомогательные интерфейсы для дженерика: 
```typescript 
interface ISuccess {
  total: number; 
}
```